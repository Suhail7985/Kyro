# Kyro Frontend

React + Vite SPA.

## Scripts

- `npm run dev` – http://localhost:5173
- `npm run build` – production build for Vercel
- `npm run preview` – preview production build

## Environment

Set `VITE_API_URL` to your backend URL (e.g. `https://your-app.herokuapp.com`).
